version: "3.7"
services:
  hermes-log:
    build:
      context: .
    image: hermes-log
    container_name: hermes-log-1
    stdin_open: true
    tty: true
    networks:
      hermes-net:
networks:
  hermes-net:
    name: hermes-network
